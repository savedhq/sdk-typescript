# sdk-typescript

TypeScript client for the Saved API, generated from the OpenAPI spec.

## Generate

Requires `../openapi/dist/openapi.yaml` (run `npm run bundle` in the `openapi` repo first).

```sh
npm run generate
```

Configuration: `openapi-ts.config.ts`
